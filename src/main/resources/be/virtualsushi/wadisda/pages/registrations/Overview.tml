<!DOCTYPE html>
<html t:type="layout" title="${message:overview.registrations}"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
      xmlns:p="tapestry:parameter">
      
      <t:grid source="registrations" row="registration" model="registrationModel" include="id,epoch,time,question" class="table table-hover" lean="true">
		<p:idCell>
        	<t:pagelink page="registrations/edit" context="registration.id.toString()">${registration.id}</t:pagelink>
       	</p:idCell>
      	<p:empty>
			<span class="badge badge-important">${message:empty.data}</span>      		
      	</p:empty>
      </t:grid>

</html>